jwt.secret=${JWT_SECRET:VGhpc0lzQVN1cGVyU2VjcmV0S2V5Rm9ySldUMTIzNDU2Nzg5}
jwt.expiration=${JWT_EXPIRATION:86400000}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/plataforma_gestion}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.cache.type=none
spring.cache.redis.time-to-live=600000
logging.level.org.hibernate.SQL=DEBUG

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:GestiaSoporte@gmail.com}
spring.mail.password=${MAIL_PASSWORD:rrka mcor ctsr twjt}

mercadopago.access-token=${MP_ACCESS_TOKEN:APP_USR-5714804539243166-060217-2da69b8ed9ee3fcc3bf3e1c80b15210d-3444576141}
mercadopago.webhook-url=${MP_WEBHOOK_URL:https://aviator-pulse-cornbread.ngrok-free.dev/api/pagos/webhook}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none

server.port=8080
app.frontend-url=${FRONTEND_URL:http://localhost:4200}

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

spring.datasource.hikari.leak-detection-threshold=60000

spring.datasource.hikari.pool-name=GestiaPool

spring.datasource.hikari.connection-test-query=SELECT 1

resilience4j.bulkhead.instances.crearPedido.max-concurrent-calls=4
resilience4j.bulkhead.instances.crearPedido.max-wait-duration=200

resilience4j.bulkhead.instances.webhookMercadoPago.max-concurrent-calls=4
resilience4j.bulkhead.instances.webhookMercadoPago.max-wait-duration=0

resilience4j.bulkhead.instances.reembolsarPago.max-concurrent-calls=2
resilience4j.bulkhead.instances.reembolsarPago.max-wait-duration=200

app.security.hsts-enabled=false

app.cupones.intervalo-compras=3

####################################################
# Flyway
####################################################

spring.flyway.enabled=true

spring.flyway.locations=classpath:db/migration

spring.flyway.baseline-on-migrate=true

spring.flyway.baseline-version=1

spring.flyway.baseline-description=Baseline Gestia

####################################################
# Storage de imágenes
####################################################
storage.provider=LOCAL
storage.local.path=${STORAGE_LOCAL_PATH:uploads}
storage.s3.endpoint=${STORAGE_S3_ENDPOINT:}
storage.s3.region=${STORAGE_S3_REGION:}
storage.s3.bucket=${STORAGE_S3_BUCKET:}
storage.s3.access-key=${STORAGE_S3_ACCESS_KEY:}
storage.s3.secret-key=${STORAGE_S3_SECRET_KEY:}
storage.s3.public-url=${STORAGE_S3_PUBLIC_URL:}

####################################################
# Upload de archivos
####################################################

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

####################################################
# Configuración de exportación de auditoría
####################################################

gestia.logs.exportacion.max-registros=1000