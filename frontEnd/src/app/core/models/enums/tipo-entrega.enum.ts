/*
 * Tipos de entrega disponibles en el backend.
 * DELIVERY: El pedido se envía al domicilio del cliente.
 * RETIRO: El cliente retira en el local.
 */
export enum TipoEntrega {
    DELIVERY = 'DELIVERY',
    RETIRO = 'RETIRO'
}
